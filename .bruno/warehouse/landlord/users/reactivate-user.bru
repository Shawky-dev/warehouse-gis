meta {
  name: reactivate-user
  type: http
  seq: 11
}

post {
  url: {{URL}}/landlord/users/{{LANDLORD_USER_ID}}/reactivate
  body: none
  auth: bearer
}

auth:bearer {
  token: {{ACCESS_TOKEN}}
}

headers {
  X-TENANT-ID: BOOTSTRAP
}

settings {
  encodeUrl: true
  timeout: 0
}
