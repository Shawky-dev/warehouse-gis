meta {
  name: deactivate-user
  type: http
  seq: 5
}

post {
  url: {{URL}}/landlord/users/{{LANDLORD_USER_ID}}/deactivate
  body: none
  auth: bearer
}

auth:bearer {
  token: {{ACCESS_TOKEN}}
}

headers {
  X-TENANT-ID: BOOTSTRAP
}

settings {
  encodeUrl: true
  timeout: 0
}
