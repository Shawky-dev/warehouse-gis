meta {
  name: login
  type: http
  seq: 1
}

post {
  url: {{URL}}/auth/login
  body: json
  auth: inherit
}

headers {
  X-TENANT-ID: BOOTSTRAP
}

body:json {
  {
    "email": "admin@system.local",
    "password": "admin123"
  }
  
}

settings {
  encodeUrl: true
  timeout: 0
}
