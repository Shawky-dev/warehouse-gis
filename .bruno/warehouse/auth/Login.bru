meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{URL}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email":"admin@system.local",
    "password":"admin123"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
