meta {
  name: reactivate-user
  type: http
  seq: 11
}

post {
  url: {{URL}}/{{TENANT_SLUG}}/users/{{TENANT_USER_ID}}/reactivate
  body: none
  auth: bearer
}

auth:bearer {
  token: {{ACCESS_TOKEN}}
}

headers {
  X-TENANT-ID: {{TENANT_SLUG}}
}

settings {
  encodeUrl: true
  timeout: 0
}
