meta {
  name: list-roles
  type: http
  seq: 6
}

get {
  url: {{URL}}/{{TENANT_SLUG}}/roles
  body: none
  auth: bearer
}

auth:bearer {
  token: {{ACCESS_TOKEN}}
}

headers {
  X-TENANT-ID: {{TENANT_SLUG}}
}

settings {
  encodeUrl: true
  timeout: 0
}
