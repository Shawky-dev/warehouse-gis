meta {
  name: list-permissions
  type: http
  seq: 8
}

get {
  url: {{URL}}/{{TENANT_SLUG}}/permissions
  body: none
  auth: bearer
}

auth:bearer {
  token: {{ACCESS_TOKEN}}
}

headers {
  X-TENANT-ID: {{TENANT_SLUG}}
}

settings {
  encodeUrl: true
  timeout: 0
}
