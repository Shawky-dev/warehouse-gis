meta {
  name: wipe-demo-data
  type: http
  seq: 4
}

delete {
  url: {{URL}}/{{TENANT_SLUG}}/dev/seed
  body: none
  auth: bearer
}

auth:bearer {
  token: {{ACCESS_TOKEN}}
}

headers {
  X-TENANT-ID: {{TENANT_SLUG}}
}
