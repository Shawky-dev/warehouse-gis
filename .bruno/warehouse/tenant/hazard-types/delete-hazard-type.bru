meta {
  name: delete-hazard-type
  type: http
  seq: 7
}

delete {
  url: {{URL}}/{{TENANT_SLUG}}/hazard-types/{{HAZARD_TYPE_ID}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{ACCESS_TOKEN}}
}

headers {
  X-TENANT-ID: {{TENANT_SLUG}}
}

settings {
  encodeUrl: true
  timeout: 0
}
