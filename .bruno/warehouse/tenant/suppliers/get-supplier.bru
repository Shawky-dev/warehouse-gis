meta {
  name: get-supplier
  type: http
  seq: 2
}

get {
  url: {{URL}}/{{TENANT_SLUG}}/suppliers/{{TENANT_SUPPLIER_ID}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{ACCESS_TOKEN}}
}

headers {
  X-TENANT-ID: {{TENANT_SLUG}}
}

settings {
  encodeUrl: true
  timeout: 0
}
