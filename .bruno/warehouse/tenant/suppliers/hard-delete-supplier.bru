meta {
  name: hard-delete-supplier
  type: http
  seq: 7
}

delete {
  url: {{URL}}/{{TENANT_SLUG}}/suppliers/{{TENANT_SUPPLIER_ID}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{ACCESS_TOKEN}}
}

headers {
  X-TENANT-ID: {{TENANT_SLUG}}
}

settings {
  encodeUrl: true
  timeout: 0
}
