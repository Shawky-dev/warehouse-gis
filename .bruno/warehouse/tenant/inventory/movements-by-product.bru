meta {
  name: movements-by-product
  type: http
  seq: 8
}

get {
  url: {{URL}}/{{TENANT_SLUG}}/inventory/movements/by-product/{{PRODUCT_ID}}?page=0&size=20
  body: none
  auth: bearer
}

auth:bearer {
  token: {{ACCESS_TOKEN}}
}

headers {
  X-TENANT-ID: {{TENANT_SLUG}}
}

settings {
  encodeUrl: true
  timeout: 0
}
