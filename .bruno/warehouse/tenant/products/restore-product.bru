meta {
  name: restore-product
  type: http
  seq: 6
}

post {
  url: {{URL}}/{{TENANT_SLUG}}/products/{{TENANT_PRODUCT_ID}}/restore
  body: none
  auth: bearer
}

auth:bearer {
  token: {{ACCESS_TOKEN}}
}

headers {
  X-TENANT-ID: {{TENANT_SLUG}}
}

settings {
  encodeUrl: true
  timeout: 0
}
