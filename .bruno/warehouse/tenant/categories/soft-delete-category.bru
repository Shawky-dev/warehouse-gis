meta {
  name: soft-delete-category
  type: http
  seq: 5
}

post {
  url: {{URL}}/{{TENANT_SLUG}}/categories/{{TENANT_CATEGORY_ID}}/soft-delete
  body: none
  auth: bearer
}

auth:bearer {
  token: {{ACCESS_TOKEN}}
}

headers {
  X-TENANT-ID: {{TENANT_SLUG}}
}

settings {
  encodeUrl: true
  timeout: 0
}
