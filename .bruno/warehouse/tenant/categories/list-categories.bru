meta {
  name: list-categories
  type: http
  seq: 1
}

get {
  url: {{URL}}/{{TENANT_SLUG}}/categories?page=0&size=20
  body: none
  auth: bearer
}

auth:bearer {
  token: {{ACCESS_TOKEN}}
}

headers {
  X-TENANT-ID: {{TENANT_SLUG}}
}

settings {
  encodeUrl: true
  timeout: 0
}
