meta {
  name: create-category
  type: http
  seq: 3
}

post {
  url: {{URL}}/{{TENANT_SLUG}}/categories
  body: json
  auth: bearer
}

auth:bearer {
  token: {{ACCESS_TOKEN}}
}

headers {
  X-TENANT-ID: {{TENANT_SLUG}}
}

body:json {
  {
    "name": "{{TENANT_CATEGORY_NAME}}",
    "code": "{{TENANT_CATEGORY_CODE}}",
    "displayName": "{{TENANT_CATEGORY_NAME}}",
    "description": "{{TENANT_CATEGORY_DESCRIPTION}}",
    "requiredZoneTypeId": null
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
