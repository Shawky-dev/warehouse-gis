meta {
  name: refresh
  type: http
  seq: 2
}

post {
  url: {{URL}}/{{TENANT_SLUG}}/auth/refresh
  body: none
  auth: inherit
}

headers {
  X-TENANT-ID: {{TENANT_SLUG}}
}

settings {
  encodeUrl: true
  timeout: 0
}
