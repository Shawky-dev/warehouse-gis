meta {
  name: refresh
  type: http
  seq: 2
}

post {
  url: {{URL}}/tenant/auth/refresh
  body: none
  auth: inherit
}

headers {
  X-TENANT-ID: {{TENANT_SLUG}}
}

settings {
  encodeUrl: true
  timeout: 0
}
