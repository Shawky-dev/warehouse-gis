meta {
  name: logout
  type: http
  seq: 3
}

post {
  url: {{URL}}/tenant/auth/logout
  body: none
  auth: inherit
}

headers {
  X-TENANT-ID: {{TENANT_SLUG}}
}

settings {
  encodeUrl: true
  timeout: 0
}
