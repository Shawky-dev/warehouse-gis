meta {
  name: get-zone-type
  type: http
  seq: 2
}

get {
  url: {{URL}}/{{TENANT_SLUG}}/zone-types/{{ZONE_TYPE_ID}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{ACCESS_TOKEN}}
}

headers {
  X-TENANT-ID: {{TENANT_SLUG}}
}

settings {
  encodeUrl: true
  timeout: 0
}
