meta {
  name: create-zone-type
  type: http
  seq: 3
}

post {
  url: {{URL}}/{{TENANT_SLUG}}/zone-types
  body: json
  auth: bearer
}

auth:bearer {
  token: {{ACCESS_TOKEN}}
}

headers {
  X-TENANT-ID: {{TENANT_SLUG}}
}

body:json {
  {
    "code": "{{ZONE_TYPE_CODE}}",
    "displayName": "{{ZONE_TYPE_NAME}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
