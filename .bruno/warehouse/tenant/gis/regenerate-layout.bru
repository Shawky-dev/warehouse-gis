meta {
  name: regenerate-layout
  type: http
  seq: 1
}

post {
  url: {{URL}}/{{TENANT_SLUG}}/gis/layout/regenerate
  body: none
  auth: bearer
}

auth:bearer {
  token: {{ACCESS_TOKEN}}
}

headers {
  X-TENANT-ID: {{TENANT_SLUG}}
}

settings {
  encodeUrl: true
  timeout: 0
}
